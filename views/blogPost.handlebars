<p>blogPost-handlebars</p>

<p>{{blog.title}} by {{blog.user.name}} on {{blog.date_created}}</p>
<p>{{blog.content}}</p>
<p>this blog id is {{blog.id}}</p>

<h2>Comments</h2>
<!-- Loop over each comment in the blog.comments array -->
{{#each blog.comments as |comment|}}
<p>{{comment.content}}</p>
<p>commented on {{comment.date_created}}</p>
<p>by {{comment.user.name}}</p>
{{/each}}

<form class="comment-form" data-id={{blog.id}}>
<div class="mb-3">
  <label for="addComment" class="form-label">Add a Comment</label>
  <textarea class="form-control" id="addComment" rows="3"></textarea>
</div>
  <button type="submit" class="btn btn-primary">Submit</button>
</form>

 <!-- JQuery Library -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"
        integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>


<script src="../js/script.js"></script>
<script src="../js/comment.js"></script>

{{#if logged_in}}
  <script src="../js/logout.js"></script>
{{/if}}




